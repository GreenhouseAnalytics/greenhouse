import { style } from "@vanilla-extract/css";

export const blankButton = style({
  appearance: "none",
  border: "none",
  background: "none",
  padding: 0,
  margin: 0,
  fontSize: "inherit",
  color: "inherit",
  cursor: "pointer",
});

export const linkButton = style({
  appearance: "none",
  border: "none",
  background: "none",
  padding: 0,
  margin: 0,
  color: "inherit",
  fontSize: "inherit",
  cursor: "pointer",
});
